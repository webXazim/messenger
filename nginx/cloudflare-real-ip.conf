# Generated by scripts/update-cloudflare-ips.sh during production deployment.
# Keeping only loopback here is safe for local config validation. Requests from
# unlisted sources retain their actual socket IP instead of trusting a spoofed
# CF-Connecting-IP header.
set_real_ip_from 127.0.0.1;
set_real_ip_from ::1;
